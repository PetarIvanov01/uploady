if ! git diff --cached --quiet --diff-filter=ACMR --; then
  git diff --cached --name-only --diff-filter=ACMR -z \
    | xargs -0 -r npx --no-install prettier --write --ignore-unknown --

  git update-index --again
fi

npm run lint
